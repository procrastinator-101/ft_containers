#ifndef VECTOR_HPP
# define VECTOR_HPP

#include <vector>
namespace ft
{
	class	vector<class T, class Alloc = allocator <T>>
	{

		public:
			explicit vector (const allocator_type& alloc = allocator_type());
	};
}

#endif